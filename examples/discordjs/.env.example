# Bot token — must match the token configured in gateway-proxy's config.json
TOKEN=your_bot_token_here

# WebSocket URL of the gateway-proxy.
# When set, all shards connect here instead of Discord's gateway directly.
# Leave unset to connect to Discord normally (useful for local development without the proxy).
GATEWAY_PROXY_URL=ws://localhost:7878
